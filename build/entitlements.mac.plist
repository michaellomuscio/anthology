<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Electron's V8 needs JIT and writable+executable memory. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>

  <!-- node-pty + native node modules ship as unsigned dylibs that need to be
       loadable inside the hardened-runtime sandbox. -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>

  <!-- Inheriting these allows child shells / claude / spawned processes to
       run their own code as expected under the hardened runtime. -->
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
</dict>
</plist>
